# Docker Compose 环境变量配置
# 使用方法: cp .env.docker.example .env.docker
# 启动命令: docker-compose --env-file .env.docker up -d

# ==================== 数据库配置 ====================
POSTGRES_DB=xunyin_db
POSTGRES_USER=xunyin_admin
# 请修改为强密码！
POSTGRES_PASSWORD=Xunyin@2026!SecurePass

# ==================== JWT 配置 ====================
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=723a5e240b42882d517ea76b8ff262ea17de90d8065e69af71ffc0ddf8dccae6

# ==================== Redis 配置 ====================
# Docker 环境默认启用 Redis（有 redis 容器）
# 如需禁用设为 false，将使用内存模式
REDIS_ENABLED=true
